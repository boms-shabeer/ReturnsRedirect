import React from 'react';
import {
  SafeAreaView,
  Text,
  useColorScheme,
  View,
  Image,
  TouchableOpacity,
} from 'react-native';

const Home = props => {

  return (
    <SafeAreaView style={{backgroundColor: '#37A585', flex: 1}}>
     
    </SafeAreaView>
  );
};

export default Home;
